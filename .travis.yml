language: python

script:
  - sudo make install
  - nohang -h
  - nohang -v
  - nohang -cc /etc/nohang/nohang.conf
  - nohang -p
  - sudo nohang -p
  - oom-sort -h
  - oom-sort
  - sudo bash -c "nohang & tail /dev/zero & sleep 30 && pkill python3"
  - sudo make uninstall
